todaydate:20260204
試用了trae來寫程式,順變叫他幫我重構,結果他一樣沒有到非常好用,還是cursor很多細節都有做到

這次改了流程,叫他改我再審閱,而不是問他後再手動抄過來,這樣大幅提升了效率,也不會亂,為每個檔案加上註解就好了

我認為我應該只要對整體程式運作流程有清晰的概念才是重點,而不是了解每行程式碼的細節

現在將程式碼變成content.tsx只負責將按鈕渲染出來
按下按鈕後,periodQueryPanel.tsx叫useCourseCrawler hook查詢課程,並將結果渲染出來

這裡要寫什麼我還再想

晚上試新的開發流程,舊流程是先搞東概念然後是步驟,然後問他每步節,再在chat裡面問程式碼,搞懂後手動複製到現有程式碼裡面
新方法,一樣是搞懂東西改念,但是不用一定要最準確的蓋概念,只要能符合故事就好,然後因此,搞懂程式碼也被加速了,而現在不再在chat裡面弄,而是直接叫agent幫我寫入我再看,並查詢,這樣不用滑來滑去,減少插入錯誤,且畫面大更好查詢,而且補上之前都沒有學的程式運行流程圖,

我應該知道到程式碼就好,且重點放在架構上,而不是細節上

試了把樣式加上,可是因為pro額度用完,他變得很笨無法正確達到我的要求,所以先都丟掉,明天再試

我發現不管什麼問題有叫他優化,回答都會好很多,所以盡量優化問題

我發現可以用vscode就好了吧,感覺trae他也沒特別好用,跟cursor還是差太多了


20260205
今天早上因為trae仍在冷卻,所以我就不用他了改用vscode(等等來刪除他好了)

用了更加激進的方法,全部都請他調整我不看具體程式碼,叫他為每部分加上註解後,我看完就繼續

我認為也可以嗎?要想一下

總之完成可以點選卡片時右邊顯示課程大綱網頁了(decard改成跳轉到網頁因為他鎖iframe內嵌)

看看哪種寫運作方法的格式較好?

要上傳時發現沒有可以選日期的,在選單增加選項,然後增加一個篩選邏輯把功能做出來